# Claim Calibration Matrix

This matrix tracks the calibration of claims in the palmprint recognition paper to ensure they are fully supported by empirical evidence, avoiding overclaiming.

| Original Claim | Problem | Evidence Available | Safer Claim | Stronger Claim if Extra Evidence Added | Paper Location |
|---|---|---|---|---|---|
| Evaluation protocols are identical and conclusions are universal. | Evaluation protocols differ in seen-identity vs held-out class and seen session directions, which reverses conclusions. | Main Tongji results show method ranking changes depending on protocol. | Evaluation protocols strongly influence component ranking and performance conclusions. | A complete protocol-sensitivity mapping across five datasets. | Introduction, Abstract, Conclusion |
| BNNeck + ArcFace consistently/universally improves performance. | In cross-session held-out class testing, BNNeck + ArcFace (M6) performs worse than CE + SupCon (M1). | Pairwise statistics show M6 minus M1 Rank-1 delta is -1.18 PP (statistically insignificant sign-flip p=0.46875). | BNNeck + ArcFace does not yield a consistent improvement over CE + SupCon under strict protocols. | Additional training seeds and cross-dataset evaluations. | Abstract, Results, Conclusion |
| BNNeck + CE (B5/M4) is the best method overall. | Its highest observed mean is direction-dependent and not universally superior. | M4 has highest mean Rank-1 on Tongji but is not statistically superior to M1 under sign-flip. | M4 exhibits the highest observed mean performance in the tested matrix but exhibits direction-dependency. | Statistical significance under larger n-seed sweeps. | Abstract, Results |
| Component matrix isolates all interaction effects. | M5 (BNNeck + CE + SupCon) is missing, so the interaction between BNNeck and SupCon is not isolated. | We have a partial component matrix (M0, M1, M2, M3, M4, M6, M7). | The component matrix isolates major individual and joint effects of heads, necks, and losses. | Complete the M5 run to isolate the full interaction. | Introduction, Method |
| Strict subject-disjoint evaluation. | The database metadata does not independently verify person-level identity beyond the manifest fields. | Audit confirms manifest-level disjointness but lacks person-level metadata. | Held-out palm-class (palm-class-disjoint) evaluation. | Person-level metadata validation via face/ID cross-referencing. | Introduction, Protocol |
| IITD validation serves as cross-session evidence. | IITD database contains only within-session images. | IITD runs are within-session only. | IITD serves as within-session secondary validation. | Obtain cross-session IITD image subsets if they exist. | Results, Limitations |
| Classical baselines are obsolete. | Gabor baseline performs competitively in some configurations. | Gabor baseline has a Rank-1 of 92.90% vs deep learning models. | Fixed Gabor baseline is included to contextualize deep learning representations under the same splits. | Evaluate advanced modern texture descriptors. | Results, Appendix |
| Complete reproducibility. | Large checkpoints and raw images cannot be committed to Git. | Pip freeze, yaml configs, and smoke test scripts are provided. | The repository includes YAML configurations, split audits, and smoke tests to reproduce metrics. | Public hosting of all model checkpoints and score tensors. | Limitations, Reproducibility |
