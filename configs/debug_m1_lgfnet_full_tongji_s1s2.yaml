# Debug Model 1: LGF-Net Full on Tongji S1 -> S2
# Safe config for RTX 4050 Laptop GPU 6GB VRAM
seed: 42
device: cuda
save_dir: experiments/debug_m1_lgfnet_full_tongji_s1s2

dataset:
  name: Tongji
  split_file: data/splits/tongji_s1_to_s2.json

sampler:
  num_identities: 4
  num_instances: 2
  fallback_identities: 4

loader:
  num_workers: 0

model:
  name: LGFNetSmall
  embedding_dim: 256
  pretrained: true

training:
  epochs: 1
  lr: 0.001
  weight_decay: 0.0001
  grad_accumulation_steps: 8
  lambda_supcon: 0.10
  temperature: 0.07
  amp: true