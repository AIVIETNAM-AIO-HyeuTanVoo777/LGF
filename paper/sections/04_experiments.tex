\section{Experimental Setup}

\subsection{Datasets and Protocols}

The primary evaluation uses the segmented Tongji palmprint dataset \cite{zhang2017tongji}. The repository manifest contains 12,000 Tongji images from 600 palms/classes, with 6,000 images in session 1 and 6,000 images in session 2. The recorded image size is $128 \times 128$. We evaluate two cross-session directions:
\begin{itemize}
    \item S1$\rightarrow$S2: training, validation, and gallery data from session 1; probe data from session 2.
    \item S2$\rightarrow$S1: training, validation, and gallery data from session 2; probe data from session 1.
\end{itemize}
The main reported Tongji numbers are bidirectional averages over S1$\rightarrow$S2 and S2$\rightarrow$S1 with seeds 42, 2026, and 2705.

IITD within split is used as a secondary validation. The repository manifest contains 2,601 IITD images from 460 palms/classes \cite{iitd_touchless_palmprint}, with all samples recorded as session 1 and image size $150 \times 150$. IITD subject-disjoint validation is reported over seeds 42, 2026, and 2705. IITD is treated as a near-saturated within-dataset setting, not as the primary contribution evidence.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\caption{Dataset split sizes and classes/subjects partitioning used by the experiments. For IITD, split sizes vary slightly across seeds; values for Seed 42 are reported.}
\label{tab:split_sizes}
\begin{tabular}{lrrrrcc}
\hline
Protocol & Train (Images) & Val (Images) & Gallery (Images) & Probe (Images) & Dev Classes & Test Classes \\
\hline
Tongji S1$\rightarrow$S2 (Disjoint) & 3,840 & 960 & 1,200 & 1,200 & 480 & 120 \\
Tongji S2$\rightarrow$S1 (Disjoint) & 3,840 & 960 & 1,200 & 1,200 & 480 & 120 \\
IITD within (Disjoint) & 1,670 & 417 & 257 & 257 & 368 & 92 \\
\hline
\end{tabular}
\end{table}

\subsection{Compared Methods}

The paper compares the following methods:
\begin{itemize}
    \item B1: ResNet18 + cross entropy + supervised contrastive learning.
    \item B2: FixedGaborResNet18.
    \item B4: ResNet18 + ArcFace.
    \item B6: ResNet18 + BNNeck + ArcFace.
    \item B7: ResNet18 + BNNeck + ArcFace + light SupCon.
\end{itemize}

\subsection{Training Recipe}

The common training recipe used by the final compared Tongji runs is:
\begin{itemize}
    \item epochs: 60;
    \item learning rate: $1 \times 10^{-4}$;
    \item weight decay: $1 \times 10^{-4}$;
    \item embedding dimension: 256;
    \item automatic mixed precision: enabled;
    \item gradient accumulation steps: 4;
    \item identity sampler: 8 identities and 2 instances per identity, with fallback identities set to 4;
    \item data loader workers: 0;
    \item Tongji seeds: 42, 2026, 2705.
\end{itemize}

B1 uses a ResNet18 baseline trained with cross entropy and supervised contrastive learning, with $\lambda_{\mathrm{supcon}}=0.10$ and temperature 0.07. B6 uses ResNet18 + BNNeck + ArcFace, with ArcFace scale $s=30.0$, margin $m=0.5$, $\lambda_{\mathrm{supcon}}=0.0$, and temperature 0.07 retained in the config for compatibility. B7 adds a light supervised contrastive term for the ablation comparison.

\subsection{Checkpoint and Evaluation Protocol}

Evaluation uses the \texttt{best.pt} checkpoint produced by the training script. For B6, the config selects the post-BN embedding for evaluation. Matching is performed with cosine similarity over normalized embeddings. The reported metrics are Rank-1, Rank-5, Macro-F1, EER, TAR@FAR=1e-2, TAR@FAR=1e-3, parameter count, FLOPs, and average inference latency. TAR@FAR=1e-3 is emphasized because it measures strict low-FAR verification behavior.

\subsection{Evaluation Metrics}

Let $x_i \in \mathbb{R}^d$ be the embedding vector extracted for image $i$. Embeddings are $L_2$-normalized: $\hat{x}_i = x_i / \lVert x_i \rVert_2$. The similarity between two images is computed using cosine similarity: $S(\hat{x}_1, \hat{x}_2) = \hat{x}_1^T \hat{x}_2$. The following evaluation metrics are defined:
\begin{itemize}
    \item \textbf{Rank-$k$ Accuracy}: The percentage of probe images for which the true matching identity is within the top-$k$ most similar gallery templates. We report Rank-1 and Rank-5 accuracies.
    \item \textbf{Macro-F1}: For each probe image, the predicted identity is the identity of the nearest gallery template under cosine similarity. Macro-F1 is computed over probe identities from these nearest-gallery predictions to summarize class-balanced identification performance.
    \item \textbf{False Accept Rate (FAR(t))} at similarity threshold $t$: The ratio of impostor pairs incorrectly accepted as genuine matches:
    \begin{equation}
        \mathrm{FAR}(t) = \frac{\#\{\text{Impostor Similarities} \ge t\}}{\#\text{Total Impostor Pairs}}
    \end{equation}
    \item \textbf{False Reject Rate (FRR(t))} at similarity threshold $t$: The ratio of genuine pairs incorrectly rejected:
    \begin{equation}
        \mathrm{FRR}(t) = \frac{\#\{\text{Genuine Similarities} < t\}}{\#\text{Total Genuine Pairs}}
    \end{equation}
    \item \textbf{True Accept Rate (TAR(t))}: The verification rate defined as $1 - \mathrm{FRR}(t)$.
    \item \textbf{Equal Error Rate (EER)}: The error rate at the threshold $t_{\mathrm{EER}}$ where FAR and FRR are equal: $\mathrm{FAR}(t_{\mathrm{EER}}) = \mathrm{FRR}(t_{\mathrm{EER}}) = \mathrm{EER}$.
    \item \textbf{TAR @ FAR}: The True Accept Rate at specific false acceptance rate constraints: $\mathrm{TAR@FAR}=10^{-2}$ and $\mathrm{TAR@FAR}=10^{-3}$.
\end{itemize}

\subsection{Hardware and Software Evidence}

The verified local evidence records CUDA execution on an NVIDIA GeForce RTX 4050 Laptop GPU with 6GB VRAM, PyTorch 2.12.0+cu126, and the Conda environment \texttt{palm\_lgf}. The current repository evidence does not track OS, CPU, or RAM details for the final B6 runs, so those fields are omitted from the paper draft.

\subsection{Reproducibility}

The experiments are configured through YAML files, split JSON files, and scripts in the repository. The relevant training and evaluation entry points are \texttt{scripts/train\_lgf.py} and \texttt{scripts/eval\_embedding.py}. Subject-disjoint split construction and audit are supported by \texttt{scripts/make\_tongji\_iitd\_disjoint\_splits.py} and \texttt{scripts/rank\_b\_split\_audit\_and\_iitd\_fix.py}. The final paper tables are recorded in \texttt{docs/results/paper\_ready\_core\_results\_tables.md}, with raw Tongji and IITD summaries in \texttt{docs/results/tongji\_subject\_disjoint\_metrics\_raw.md} and \texttt{docs/results/iitd\_subject\_disjoint\_summary.md}. Raw segmented data, experiment directories, and checkpoint tensors are not committed; the repository tracks configs, split JSON files, metadata, scripts, and saved result summaries.
